## v0.2.1 - 2026-08-23

本版本重点补齐 Windows 桌面体验：自定义标题栏、窗口控件，以及 WebView2 内的文件拖放。同时修复融合终端粘贴同步，并提高 Agent 多步工具调用上限。

### 新增：

- 新增 Windows 风格窗口控制按钮（最小化 / 最大化 / 还原 / 关闭），macOS 仍使用红绿灯样式。

### 优化：

- 标题栏与拖拽区域高度由 30px 调整为 40px，Monaco 编辑器、服务器概览等内容区同步适配。
- 主窗口默认尺寸由 1200×800 调整为 1500×1000。
- 快捷按钮组尺寸放大；Windows 下移至标题栏左侧，避免与窗口控制按钮重叠。
- 终端 Tab 左侧留白按平台区分：macOS 避让红绿灯，Windows 根据概览面板显隐调整。
- 加大服务器 Tab 右侧留白，避免与放大后的快捷按钮重叠。
- Skills 管理窗口的操作按钮移至编辑器底部工具栏，降低头部占用。
- Agent 图递归上限提升至 100，降低多步工具调用提前中断的概率。
- 重写 README 功能说明与截图布局，并更新安装说明 PDF。
- 设置页调整面板宽度时，自动保存配置到本地。
- 自动重新加载上次退出时的窗口宽高。

### 修复：

- 修复 Windows 下服务器树与 SFTP 文件拖拽失效：用指针事件模拟 WebView2 内部拖放，并补齐拖动光标与事件清理。
- 修复拖动未选中文件行时选择不正确的问题。
- 修复融合终端组内粘贴不同步：组会话粘贴改为走终端数据通道，同步写入其他终端。

## v0.2.0 - 2026-08-22

本版本新增面向 Linux 运维的 Agent，可在 SSH 会话侧栏中对话、执行命令并加载运维技能。

### 新增：

- 新增 Linux 运维 Agent 面板，单机终端与融合终端均可打开侧栏对话。
- 新增 Agent 多会话标签与历史记录，会话按服务器分组持久化，超过 30 天未活动自动清理。
- 新增流式对话展示：思考过程、工具调用时间线、命令队列、Markdown 回复，以及停止生成。
- 新增远程命令执行：支持静默后台执行或写入当前终端可视化执行；常驻命令可由观察子 Agent 判断结束时机，并支持按执行 ID 取消。
- 新增访问限制（请求批准 / 帮我批准 / 高度自主 / 完全访问），与 R0–R4 风险分级对齐；高风险操作可先询问再执行。
- 新增 OpenAI 兼容模型配置，支持多模型条目、API Key、上下文窗口和推理深度；设置页增加独立 Agent 标签，配置与提示词支持热更新。
- 新增用户 Skills 管理窗口，内置运维专题文档与磁盘检查技能，支持按需加载；升级只替换内置文档，保留用户自定义技能。
- 新增对话附件：支持选择本地文件、粘贴截图，以及上下文用量展示与自动 / 手动压缩。
- 新增空会话常用问题引导，支持自定义快捷提问。

### 优化：

- 优化列宽拖拽，支持从右侧面板反向调整 Agent 面板宽度。
- 优化确认弹窗，支持无输入框的危险 / 警告确认场景。
- 统一加载动画样式，全局、SFTP 树与传输列表共用同一旋转指示。
- 将终端心跳间隔调整为 10 秒，监控核心指标轮询间隔调整为 2 秒。
- 补充毛玻璃与拟态主题下 Agent 面板、会话栏、输入区和设置页样式。

### 修复：

- 修复终端 Ctrl+V 粘贴改为走终端写入通道，与 Agent 可视化执行共用同一输入链路。
- 修复关闭会话时列表更新不够及时的问题。
- 修复 WebView 粘贴事件拿不到截图时，改为从系统剪切板读取位图。

### 发布：

- 版本号升级至 `0.2.0`，同步更新 `package.json`、`Cargo.toml` 和 Tauri 配置。
- 安装包随版本同步内置 Agent 提示词与技能文档。

## v0.1.7 - 2026-08-06

### 优化：

- 统一远程文件排序规则，远程文件列表和扫描结果按资源管理器风格排序，磁盘列表按路径字典序排序。
- 优化 SFTP 上传缓冲策略，按服务器 CPU / 内存动态调整读取缓冲，提升弱机上传稳定性。
- 优化终端心跳：连接成功后再启动 ping，并将间隔调整为 5 秒；过期会话仅通知关闭，避免提前从会话表移除。

### 修复：

- 修复终端 resize 节流导致最终尺寸不同步，尺寸稳定后补一次 fit，避免底部被 overflow 裁切。
- 修复 Monaco 编辑器在多服务器同路径场景下打开为空的问题，按唯一 key 区分远程文件 model。
- 修复文件管理空白处无法唤出右键菜单，并在传输项中展示本地 / 远程路径。
- 修复上传暂停 / 取消不够及时的问题，支持取消并删除远程文件。
- 修复慢速传输场景下网速与剩余时间计算不稳定的问题。
- 修复终端断线后的 UI 反馈，收到断开信号时正确关闭会话并更新状态。

## v0.1.6 - 2026-07-03

### 新增：

- 新增融合监控，支持从服务器列表或独立选择窗口批量打开多台服务器的监控面板。
- 新增融合监控配置栏，支持选择系统、进程、网络、磁盘模块，并可调整监控卡片宽度、磁盘区域高度和磁盘路径过滤。

### 优化：

- 优化服务器监控面板结构，将系统信息、进程、网络和磁盘拆分为独立模块，便于单机监控和融合监控复用。
- 优化监控数据状态管理，监控数据改为绑定到具体 SSH 会话实例，支持多台服务器同时轮询展示。
- 优化监控轮询逻辑，按会话实例独立启动和清理定时器，减少切换会话时的数据串扰。
- 优化主题样式，补充融合监控在毛玻璃主题和拟态主题下的卡片、配置栏、按钮和输入框样式。

## v0.1.5 - 2026-07-02

### 新增：

- 新增终端标签右键菜单，支持关闭、关闭其他、关闭全部、新窗口打开和融合到主窗口。
- 新增终端断开连接提示，连接中断后按键可自动触发重连。

### 优化：

- 优化终端跨窗口移动能力，移动时同步保留终端、SFTP 等会话状态，减少跨窗口切换后的状态丢失。
- 优化多窗口主从关系管理和空窗口回收逻辑，主窗口保留服务器列表，非主窗口在最后一个标签关闭后自动销毁。
- 优化终端标签拖拽交互，补充拖拽数据，减少拖动时误触发文本选中的问题。
- 统一输入组件交互行为，补齐 Enter / Escape 键盘处理，提升重命名和输入操作的一致性。
- 优化终端滚动体验，禁用用户输入时的自动滚动，减少输入过程中视图跳动。
- 更新应用图标与部分功能截图资源。

### 修复：

- 修复 SFTP 打开空文件时被错误过滤的问题，支持空文件进入编辑器或通过系统应用打开。
- 修复空文件经系统应用编辑后的回传问题，支持修改后同步写回远端文件。
- 修复 SFTP 拖拽上传在多标签、多窗口场景下的误处理问题，仅响应当前激活会话。
- 修复终端粘贴路径时的输入异常，改为通过终端输入链路写入剪贴板内容。
- 修复终端断线后的交互异常，在连接断开时输出提示并恢复重连触发逻辑。

## v0.1.4 - 2026-06-12

### 优化：

- 优化终端标签拖拽逻辑，修复分组场景下的命令分发异常
- 优化拟态主题与快捷按钮组样式，提升界面视觉表现
- 统一输入框交互行为，修复终端搜索状态更新问题，并改进部分 SFTP 操作体验
- 禁用 macOS 新窗口原生全屏行为，优化窗口打开体验

## v0.1.3 - 2026-06-02

### 新增：

- 新增版本同步脚本 `pnpm set-version`，统一更新前端、Tauri 配置和 Cargo 版本号。
- 新增 macOS DMG 打包脚本和安装说明资源

### 优化：

- 优化 README 功能说明，补充 SSH 终端、SFTP 文件管理、服务器管理和界面能力介绍。
- 优化 Windows 下拖拽坐标计算，适配系统显示缩放后的文件拖拽定位。
- 优化远程命令退出码处理，由前端根据输出内容决定展示状态。

### 修复：

- 修复 SFTP 下载和上传的暂停、取消、覆盖、续传、进度上报和完成回调问题。
- 修复 SFTP 上传入口、目录上传相对路径转换、上传冲突处理和上传后刷新问题。
- 修复 SFTP 文件表格和目录树拖拽状态残留、`dragend` 过早清空导致 `drop` 失效的问题。
- 修复 eventBus 的 `on`/`off` 监听闭环问题，避免 SFTP、服务器磁盘和融合终端事件误解绑或残留。
- 修复远程文件解析空行导致的边界问题。

### 发布：

- 版本号升级至 `0.1.3`，同步更新 `package.json`、`Cargo.toml`、`Cargo.lock` 和 Tauri 配置。

## v0.1.2 - 2026-05-27

### 新增：

- 支持融合终端。

### 修复：

- 修复服务器tab栏全屏改变时左边距未及时更新的问题
- 修复mac端字啊终端快捷键事件未监听问题

## v0.1.1 - 2026-05-26

### 修复：

- 修复窗口初始化数据传递问题。
- 设置数据同步问题。

## v0.1.0 - 2026-05-26

这是 Keray Shell Rust 的首个开源预览版本，提供基础 SSH/SFTP 桌面客户端能力，适合体验核心工作流和参与早期反馈。

### 新增

- 支持 SSH 终端连接与多会话切换。
- 支持 SFTP 远程目录浏览、文件列表和传输任务展示。
- 支持服务器分组、最近连接和快速搜索。
- 支持密码、私钥连接配置。
- 支持服务器数据导入、导出与同步。
- 支持拟态、毛玻璃主题，以及终端字体、字号、布局偏好。
- 集成 Monaco Editor，支持远程文本文件在线编辑。

### 开源准备

- 新增 MIT License。
- 新增 README、贡献指南、安全政策、行为准则和 issue/PR 模板。
- 移除随仓库分发的 Menlo 字体文件，避免第三方字体授权风险。

### 已知限制

- 本地密码/私钥存储方案仍需要进一步安全评估。
- 部分平台窗口效果和字体表现可能存在差异。
- CI、自动构建和正式安装包签名流程仍待完善。

### 校验

- `pnpm build`
- `cargo check`
