# 磁盘诊断输出格式

1. 先一句话结论：是容量、inode、已删未释放、IO，还是硬件/只读。
2. 指出出问题的挂载点或设备，例如 `/` 95%、`/var` inode 100%。
3. 只给接下来最有用的 1～3 条命令；用户已贴输出时，基于输出分析，不要重复同一条。
4. 清理建议必须带路径、预估收益和风险。高危删除（`/var/lib/docker`、数据库目录、用户家目录）只提示，不直接给破坏性命令。
5. 所有命令默认只读。需要 `rm`、`truncate`、`fstrim`、重启服务、`fsck` 时单独说明影响并等待确认。
